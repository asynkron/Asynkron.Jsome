<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    
    <!-- Global Tool Configuration -->
    <PackAsTool>true</PackAsTool>
    <ToolCommandName>swaggergen</ToolCommandName>
    <PackageId>dotnet-swaggergen</PackageId>
    
    <!-- Package Metadata -->
    <Description>A C# code generator that processes Swagger 2.0 JSON files to produce C# DTO classes and FluentValidation validators</Description>
    <PackageTags>swagger;openapi;codegen;dto;fluentvalidation;dotnet-tool</PackageTags>
    <PackageProjectUrl>https://github.com/asynkron/SwaggerGen</PackageProjectUrl>
    <RepositoryUrl>https://github.com/asynkron/SwaggerGen</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <Authors>asynkron</Authors>
    <Company>asynkron</Company>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Newtonsoft.Json" Version="13.0.4" />
    <PackageReference Include="Handlebars.Net" Version="2.1.6" />
    <PackageReference Include="FluentValidation" Version="11.9.0" />
    <PackageReference Include="YamlDotNet" Version="16.3.0" />
  </ItemGroup>

  <ItemGroup>
    <None Include="Templates\**\*" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

</Project>
