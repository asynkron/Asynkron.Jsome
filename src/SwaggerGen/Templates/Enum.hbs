namespace {{Namespace}};

/// <summary>
/// {{Description}}
/// </summary>
public enum {{EnumName}}
{
{{#each Values}}
    /// <summary>
    /// {{Description}}
    /// </summary>
    {{Name}} = {{ValueLiteral}}{{#unless @last}},{{/unless}}

{{/each}}
}
